{
  "name": "iota-sprint-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "IOTA team sprint SP tracker and per-developer burndown",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "start": "npm run dev"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "concurrently": "9.1.2"
  }
}
